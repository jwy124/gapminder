# 데이터 품질 확인 보고서 (Data Quality Report)

- **대상 파일**: `data/gapminder.csv`
- **점검 스크립트**: [`clean.R`](../clean.R) (base R, 외부 패키지 의존 없음)
- **검사 행 수**: 1,704 (142개국 × 12개 연도)
- **검사 일시**: 2026-06-27 10:42:52
- **종합 판정**: ✅ **통과 — 치명적 품질 문제 없음** (확인 필요 항목 1건)

---

## 종합 요약

| # | 점검 영역 | 결과 |
|---|-----------|------|
| 1 | 구조 (컬럼명·타입) | ✅ 정상 |
| 2 | 결측치 (NA·빈 문자열) | ✅ 없음 |
| 3 | 중복 (완전중복·패널 키) | ✅ 없음 |
| 4 | 값 범위/유효성 | ⚠️ 확인 1건 (`pop` 비정수) |
| 5 | 범주형 일관성 (대륙·국가 매핑) | ✅ 정상 |
| 6 | 패널 균형성 | ✅ 균형 패널 |
| 7 | 이상치 (IQR) | ℹ️ 참고용 (오류 아님) |

---

## 1. 구조 (Structure)

- 행 수: **1,704**, 열 수: **6**
- 컬럼: `country`, `year`, `pop`, `continent`, `lifeExp`, `gdpPercap` — 기대한 6개 컬럼 모두 존재 ✅

| 컬럼 | 타입 | 판정 |
|------|------|------|
| country | character | ✅ |
| year | integer | ✅ 숫자형 |
| pop | numeric | ✅ 숫자형 |
| continent | character | ✅ |
| lifeExp | numeric | ✅ 숫자형 |
| gdpPercap | numeric | ✅ 숫자형 |

## 2. 결측치 (Missing values)

- 전체 컬럼 **NA 0개** ✅
- 문자형 컬럼(`country`, `continent`) **빈 문자열 0개** ✅

## 3. 중복 (Duplicates)

- 완전 중복 행: **0개** ✅
- `(country, year)` 조합: **유일** — 패널 데이터 키 무결성 OK ✅

## 4. 값 범위 / 유효성 (Range & validity)

| 변수 | 범위 | 판정 |
|------|------|------|
| year | 1952 ~ 2007 | ✅ 정수, 현실적 범위 |
| lifeExp | 23.60 ~ 82.60 | ✅ (0, 120] 범위 내 |
| pop | 60,011 ~ 1,318,683,096 | ✅ 모두 양수 |
| gdpPercap | 241.17 ~ 113,523.13 | ✅ 모두 양수 |

> ⚠️ **확인 필요 — `pop` 비정수값 1건**
> **China 1952**의 인구가 정수가 아님. 표시값은 556,263,528이지만 실제 저장값은 **556263527.999989** (소수부 0.999989).
> → 이는 `clean.R`의 오류가 아니라 **Gapminder 원본 데이터셋 자체에 존재하는 알려진 부동소수점 흔적**(원본 생성 과정의 round-trip 부작용)이다.
> → 분석 결과에는 영향 없음. 정리하려면 `df$pop <- round(df$pop)` 한 줄로 정수화 가능.

## 5. 범주형 일관성 (Categorical consistency)

- 대륙 (5개): **Africa, Americas, Asia, Europe, Oceania** — 표준 5개 범주 내, 표기 일관 ✅
- 국가 수: **142개** — 앞뒤 공백 없음 ✅
- 국가-대륙 매핑: **각 국가는 단일 대륙에 매핑** — 매핑 불일치 없음 ✅

## 6. 패널 균형성 (Panel balance)

- 관측 연도 (12개): 1952, 1957, 1962, 1967, 1972, 1977, 1982, 1987, 1992, 1997, 2002, 2007
- **균형 패널** — 모든 국가가 12개 연도를 빠짐없이 보유 ✅
- 연도 간격 **일정(5년)** ✅

## 7. 이상치 탐지 (Outliers — IQR 1.5×, 참고용)

| 변수 | 이상치 후보 | 정상 범위 (IQR 규칙) |
|------|-------------|----------------------|
| lifeExp | 0개 | [14, 105] |
| gdpPercap | 143개 | [-10,983, 21,511] |
| pop | 208개 | [-22,393,673, 44,772,558] |

> ℹ️ 이들은 **오류가 아닌 실제 극단값**(예: 쿠웨이트의 석유 기반 고GDP, 중국·인도의 대규모 인구)일 가능성이 높아 **자동 제거하지 않는다.** 분석 목적에 따라 로그 변환 등으로 처리할 것을 권장.

---

## 결론 및 권고

1. **데이터는 분석에 즉시 사용 가능한 상태**다. 결측·중복·범주 오류·패널 불균형이 없는 깨끗한 균형 패널 데이터.
2. 유일한 확인 항목인 `pop` 비정수값은 원본의 알려진 특성으로, 정수 인구가 필요하면 `round()` 처리만 하면 된다.
3. 소득(`gdpPercap`)·인구(`pop`)는 분포가 우편향이므로, 통계 분석 시 **로그 변환** 또는 **중앙값 기반 비교**를 권장한다.

---

*본 보고서는 `clean.R` 실행 결과를 기록한 것이다. 재현하려면 프로젝트 루트에서 `Rscript clean.R`을 실행한다.*
